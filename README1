
### 打开UI页面

node bin/menu-tester.js serve

node bin/menu-tester routes import routes.json

node bin/menu-tester test --mode route

# 使用配置文件测试
menu-tester test --config config.json

# 路由模式测试
menu-tester routes import routes.json
menu-tester test --mode route

# AI 模式测试
menu-tester test \
  --url "https://admin.example.com" \
  --token "your-token" \
  --mode ai

# 混合模式测试
menu-tester test --mode hybrid --config config.json

